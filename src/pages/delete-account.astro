---
import Legal from '../layouts/Legal.astro';
import { dictionary } from '../i18n';
import { defaultLocale } from '../i18n/locales';

const t = dictionary(defaultLocale);
---

<Legal
  entry="delete-account"
  page="delete-account"
  description={t.legal.deleteAccount.description}
/>
